# [ Plugins ]
set -g @plugin "tmux-plugins/tpm"
set -g @plugin "jaclu/tmux-menus"
set -g @plugin "joshmedeski/tmux-nerd-font-window-name"

# [ Source/Run Extra Files ]
# Source core options
source-file "~/.config/tmux/core.tmux"

# Source Keymaps
source-file "~/.config/tmux/keymaps.tmux"

# Source colorscheme configuration
source-file "~/.config/tmux/colorscheme.tmux"

# Source status line configuration
source-file "~/.config/tmux/status-line.tmux"

# Initialize Tmux Plugin Manager
run-shell "~/.config/tmux/plugins/tpm/tpm"

