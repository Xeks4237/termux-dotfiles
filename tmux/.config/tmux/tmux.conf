# [ Plugins ]
# Tmux Plugin Manager itself
set -g @plugin 'tmux-plugins/tpm'

# Plugin which adds custom nerd font icons to window names
set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'

# [ Source/Run Extra Files ]
# Source core options
source-file "~/.config/tmux/core/*"

# Source colorscheme configuration
source-file "~/.config/tmux/colorscheme.tmux"

# TODO: Fork tpm and make it "NORMAL"
# if-shell '[ -d "${XDG_DATA_HOME:-$HOME/.local/share/}/tmux/plugins/tpm/" ]' {
# 	run-shell '${XDG_DATA_HOME:-$HOME/.local/share/}/tmux/plugins/tpm/bin/install_plugins'
# } {
# 	display-message 'Installing TPM for tmux...'
# 	run-shell 'git clone --depth=1 https://github.com/tmux-plugins/tpm "${XDG_DATA_HOME:-$HOME/.local/share/}/tmux/plugins/tpm/" && ${XDG_DATA_HOME:-$HOME/.local/share/}/tmux/plugins/tpm/bin/install_plugins'
# }

# Initialize Tmux Plugin Manager
run-shell '~/.config/tmux/plugins/tpm/tpm'

# Source file with configurations for plugins
source-file "~/.config/tmux/plugins.tmux"
